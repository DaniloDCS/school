extends ../layout
block content
  | #{message}
  h1 PDF tools 

  br

  div 
    h4 Compress PDF
    form(action="/apps/pdf/compress" method="post" enctype="multipart/form-data")
      input(type="file" name="file")
      input(type="submit" value="Compress") 